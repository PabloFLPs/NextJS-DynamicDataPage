import styles from "../styles/Home.module.css"

export const Home = (props) => {

    return (
        <div className={styles.main}>
            <h1>WHY CHOOSE US</h1>
            <span>Moles appear in all shapes and sizes? We can help!</span>
        </div>
    )
}
